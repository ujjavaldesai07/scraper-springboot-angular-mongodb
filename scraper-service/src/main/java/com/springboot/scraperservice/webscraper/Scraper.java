package com.springboot.scraperservice.webscraper;

/**
 * Interface to create the required methods, as we iterating over the enum
 * and expecting that the method is present.
 */
public interface Scraper {
    void startScraper();
}
